{
  "name": "school-finder-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "school-finder"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=school-finder",
    "build": "npm run build --workspace=school-finder",
    "start": "npm run start --workspace=school-finder",
    "lint": "npm run lint --workspace=school-finder",
    "test": "npm run test --workspace=school-finder"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}